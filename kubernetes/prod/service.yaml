apiVersion: v1
kind: Service
metadata:
  namespace: defi-providers
  name: defi-providers
spec:
  ports:
    - name: grpc
      port: 3002
      targetPort: 3002
  selector:
    component: defi-providers
